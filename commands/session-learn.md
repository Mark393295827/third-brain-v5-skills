---
name: session-learn
description: Extract knowledge from the current session. Invoke with /session-learn or "extract this session".
---
Run the `session-learn` skill — scan session for new concepts, entities, corrections, patterns, decisions, and gaps.

Resolve destinations from `system/config.md` when present. The run is successful only when saved items are classified by signal type, written to durable notes or explicitly skipped, and the final summary separates saved files from open questions.
